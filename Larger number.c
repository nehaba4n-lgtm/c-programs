#include <stdio.h>
int main (){
    int x , y;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    if (x > y)
        printf("%d is greater",x);
    else
        printf("%d is greater",y);
    return 0;
}
